{
  "name": "@takosjp/yurumeet",
  "version": "0.1.2",
  "license": "AGPL-3.0-only",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "bunx vite --config vite.config.ts",
    "dev:mock": "bun scripts/dev-mock.ts",
    "dev:mock:api": "bun scripts/dev-mock-server.ts",
    "dev:site": "bunx serve site",
    "check": "bun run fmt:check && bun run lint && bun run test && bun run build:takos-worker",
    "deploy": "bun scripts/deploy.mjs",
    "test": "bun test src/ scripts/worker-entry-contract.test.ts scripts/product-identity-contract.test.ts scripts/opentofu-output-contract.test.ts scripts/release-version.test.ts scripts/runtime-config-contract.test.ts scripts/takoform-capsule.test.ts scripts/site-install-surface.test.ts scripts/check-core-release.test.ts && bun scripts/dev-mock-server.ts --self-test",
    "build": "bun run build:client",
    "build:client": "bunx vite build --config vite.config.ts",
    "build:takos-worker": "bun scripts/build-takos-worker.ts",
    "check:core-release": "bun scripts/check-core-release.mjs",
    "smoke:postdeploy": "bun scripts/post-deploy-smoke.ts",
    "lint": "tsc --noEmit && bun scripts/check-brand-assets.mjs",
    "fmt": "bunx prettier --write package.json index.html vite.config.ts src site scripts README.md README.en.md",
    "fmt:check": "bunx prettier --check package.json index.html vite.config.ts src site scripts README.md README.en.md"
  },
  "cloudflare": {
    "bindings": {
      "ENCRYPTION_KEY": {
        "description": "Required 64-character hex key used to encrypt sensitive Yurumeet data. Generate one with `openssl rand -hex 32`."
      },
      "AUTH_PASSWORD_HASH": {
        "description": "Required password credential for a direct Cloudflare install. Use a PBKDF2 salt:hash value or a strong one-time bootstrap secret."
      }
    }
  },
  "dependencies": {
    "@solidjs/router": "^0.15.0",
    "@takosjp/yurucommu-api": "^3.1.0",
    "@takosjp/yurucommu-core": "^3.1.0",
    "html5-qrcode": "^2.3.8",
    "qrcode-generator": "^2.0.4",
    "solid-js": "^1.9.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260420.1",
    "@types/bun": "latest",
    "@types/node": "^22.0.0",
    "esbuild": "^0.25.12",
    "typescript": "^5.7.0",
    "vite": "^6.4.1",
    "vite-plugin-solid": "^2.11.0",
    "wrangler": "^4.103.0"
  }
}
